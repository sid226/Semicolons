- deployment: string        # instead of job keyword, use deployment keyword
  pool:
    name: string
    demands: string | [ string ]
  environment: ubuntu
  strategy:
    runOnce:
      deploy:
        steps:
        - script: echo Hi!
